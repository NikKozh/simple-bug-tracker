# Значение по-умолчанию вместе с Option[Int] нужно для того, чтобы не было необходимости явно передавать аргумент
# в вызывающем коде, когда этот аргумент не нужен, и при этом сохранялась возможность ввода аргумента в строке браузера
GET     /                           controllers.Application.index(id: Option[Int] ?= None)
POST    /                           controllers.Application.createTask
POST    /:id/update                 controllers.Application.updateTask(id: Int)
POST    /:id/delete                 controllers.Application.deleteTask(id: Int)

# Здесь используется пустая строка вместо Option[String] = None, т.к. пустая строка означает отсутствие запроса
# от пользователя (форма не даст отправить пустой запрос), а значит, нам не нужно выводить информацию
# о том, что ничего не найдено:
GET     /search                     controllers.SearchController.search(query: String ?= "")

# TODO: по возможности заменить устаревшее versioned на инъекцию
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)